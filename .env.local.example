# Copier ce fichier en .env.local et renseigner les vraies clés.
# .env.local n'est JAMAIS envoyé sur GitHub (voir .gitignore).

# Clé API-Football (https://www.api-football.com/) — sert à récupérer les matchs.
APIFOOTBALL_KEY=

# Clé API Anthropic (https://console.anthropic.com/) — sert à générer les analyses IA.
ANTHROPIC_API_KEY=
